{
  "schema_version": 3,
  "codex_cli": {
    "tested_version_ref": "build/version.json:codex_cli_tested"
  },
  "evidence": {
    "official_desktop_documentation": "https://learn.chatgpt.com/docs/quickstart#setup",
    "official_desktop_merge_documentation": "https://learn.chatgpt.com/docs/whats-new#use-codex-in-the-chatgpt-desktop-app",
    "official_desktop_command_documentation": "https://learn.chatgpt.com/docs/developer-commands#codex-app",
    "official_pinned_cli_desktop_source": "https://github.com/openai/codex/blob/rust-v0.147.0/codex-rs/cli/src/desktop_app/mac.rs",
    "permission_profile_compatibility_basis": "Official mixed-version permission-profile compatibility threshold",
    "official_agents_documentation": "https://learn.chatgpt.com/docs/agent-configuration/agents-md#how-codex-discovers-guidance",
    "official_configuration_precedence": "https://learn.chatgpt.com/docs/config-file/config-basic#configuration-precedence",
    "official_release": "https://github.com/openai/codex/releases/tag/rust-v0.147.0",
    "official_standalone_installer": "https://github.com/openai/codex/releases/download/rust-v0.147.0/install.sh",
    "official_permissions_documentation": "https://learn.chatgpt.com/docs/permissions"
  },
  "software_install": {
    "desktop_configuration_binding": "not-guaranteed-by-codex-app",
    "desktop_delegate": "codex app",
    "desktop_documented_product": "ChatGPT",
    "desktop_pinned_cli_packaging": "Codex.app/Codex.dmg",
    "installer_asset": "install.sh",
    "installer_release": "rust-v0.153.1",
    "installed_version": "0.153.1",
    "standalone_layout": "packages/standalone"
  },
  "verified_version_ref": "build/version.json:codex_cli_tested",
  "verified_at": "2026-09-03T22:15:16+00:00",
  "native_surfaces": {
    "verified_at": "2026-08-31",
    "config_home": "~/.codex",
    "surfaces": [
      {
        "path": "AGENTS.md",
        "kinds": [
          "instruction"
        ],
        "shape": "file",
        "source": "https://learn.chatgpt.com/docs/agent-configuration/agents-md",
        "evidence": "ran",
        "note": "\n\n**Searched in the product's own pinned bytes on 2026-08-29 and not found, which argues nothing either way.** Fixed-string, anchored to this product's configuration home -- the bare leaf name is in every one of these binaries and proves nothing, so only the anchored form counts. An invented path was searched in the same run and was also absent, so the search discriminates.\n\nThis row stays `page` because **a path built by joining a directory to a name at runtime never appears as a literal**, and that is the shape of every remaining one. Moving it off `page` needs the product run against a target and asked what it resolved, not a deeper grep.\n\n**Off `page` on 2026-08-31, by running the product and asking what it resolved -- which is what this row said it would take.** The pinned `codex-0.151.0-linux-x64.tgz` was fetched, its digest checked against the artifact table, and `codex debug prompt-input` run against a temporary `CODEX_HOME`. That command renders the model-visible input as JSON, and a marker written into the instruction file at the configuration home is in it.\n\n**Two controls, because one proves nothing.** A marker never written to any file is absent from the same output, so the search discriminates; and removing the file makes the marker disappear, so the result is about this file rather than about anything else the command renders. The earlier session was right that a deeper grep could not settle this -- the instrument was a command nobody had looked for."
      },
      {
        "path": "config.toml",
        "kinds": [
          "setting"
        ],
        "shape": "file",
        "source": "https://learn.chatgpt.com/docs/config-file/config-reference; anchored literal measured in the pinned artifact by scripts/evidence.py",
        "evidence": "bytes",
        "note": "MCP servers live here under [mcp_servers.<name>]; a key inside a file is not a projection surface, so mcp is not declared Confirmed 2026-08-29 by `scripts/evidence.py`, which installs the pinned artifact into a temporary prefix and searches the installed bytes for each declared namespace joined to the configuration home the binary itself documents -- 11 occurrence(s) of `.codex/config.toml`. A namespace no product can own is searched alongside the real ones in the same pass and was absent, which is what separates a reading from a search that matches everything."
      },
      {
        "path": "hooks.json",
        "kinds": [
          "hook"
        ],
        "shape": "file",
        "source": "https://learn.chatgpt.com/docs/config-file/config-reference",
        "evidence": "page",
        "note": "\n\n**Searched in the product's own pinned bytes on 2026-08-29 and not found, which argues nothing either way.** Fixed-string, anchored to this product's configuration home -- the bare leaf name is in every one of these binaries and proves nothing, so only the anchored form counts. An invented path was searched in the same run and was also absent, so the search discriminates.\n\nThis row stays `page` because **a path built by joining a directory to a name at runtime never appears as a literal**, and that is the shape of every remaining one. Moving it off `page` needs the product run against a target and asked what it resolved, not a deeper grep.\n\n**Driven 2026-08-30 against the 0.151.0 artifact on linux/x86_64, and the result is a negative worth recording.** A `hooks.json` was placed in a temporary CODEX_HOME in four states -- a valid one-command `SessionStart` hook, a file that is not JSON at all, valid JSON under an invented event name, and valid JSON with only an invented top-level key -- and `codex doctor` produced **byte-identical output in all four, and in the state where the file is absent**. Not a startup warning, not a parse complaint, not a count.\n\nSo there is no credential-free way to ask this product whether a hook file was found, parsed, trusted or active. That is the reason this row stays `page` and it is a better reason than the grep it had: the path is not confirmed, and the thing that would confirm it does not answer.\n\n**It also settles what a setup carrying hooks may promise.** The vendor is explicit that a non-managed handler runs only once a person has trusted its exact hash, so an install puts bytes in place and leaves the handlers inert, and a *restore* of a previously trusted file does not restore trust. This estate ships no hooks in any setup today, so nothing currently makes the claim -- but a consumer installing a hook component does, and the four states above are the reason it cannot be checked from here.\n\n**Re-tried 2026-08-31 and unchanged.** Also measured in the bytes this time: the anchored form is absent, and what the binary does carry is `\"hooks\": \"./hooks.json\"` inside a *plugin manifest* template and the literal `hooks/hooks.json` in a string table. Both describe a hooks file relative to a **plugin**, which is consistent with this estate's finding that a hook reaches this product through a plugin rather than through a file of its own -- and neither is evidence about a `hooks.json` at the configuration home, which is what this row is about. Recorded so the two are not confused: a file with the same leaf name in a different parent is a different surface.\n\n**Attempted 2026-08-31 and it stayed `page`, with what was tried recorded so the next reader does not repeat it.** Two routes, both dead ends, and the second is the instructive one:\n\n* A deeper grep does not move it. The pinned `codex-0.151.0-linux-x64.tgz` was fetched and digest-checked, and `prompts` appears 184 times -- as an English word. The *joined* literal never appears, because a path built from a home and a name at runtime is not a literal in the artifact. This is the shape of every row still at `page`, here and in antigravity: bare-name hits of 626 for `plugins` and 1059 for `agents` prove nothing about a path.\n* Running the product did not answer either, **and it did not answer *no*.** A marker prompt was planted at `$CODEX_HOME/prompts/nddev-marker.md` with a control at `$HOME/.codex-not-a-home/prompts/`, and `codex debug prompt-input` returned **neither**. `codex doctor` reports config, MCP and runtime and names no prompt or hook. An instrument silent about the control as well as the subject has measured nothing -- reading the marker's zero alone would have produced a false negative about a surface the vendor documents.\n\nSo the row is correct at `page`, which is what `page` is for."
      },
      {
        "path": "prompts",
        "kinds": [
          "command"
        ],
        "shape": "directory",
        "source": "https://learn.chatgpt.com/docs/custom-prompts",
        "evidence": "page",
        "note": "$CODEX_HOME/prompts/*.md, each file a slash command\n\n**Searched in the product's own pinned bytes on 2026-08-29 and not found, which argues nothing either way.** Fixed-string, anchored to this product's configuration home -- the bare leaf name is in every one of these binaries and proves nothing, so only the anchored form counts. An invented path was searched in the same run and was also absent, so the search discriminates.\n\nThis row stays `page` because **a path built by joining a directory to a name at runtime never appears as a literal**, and that is the shape of every remaining one. Moving it off `page` needs the product run against a target and asked what it resolved, not a deeper grep.\n\n**Tried again on 2026-08-31 and still `page`, with the routes now named so the next attempt starts past them.** `doctor` reports `config.toml` and nothing else of the home; `debug prompt-input`, which does carry `AGENTS.md`, does not carry a prompt placed at `$CODEX_HOME/prompts/` -- these are slash commands offered in the interface, not model input, so its silence argues nothing; `debug app-server` exposes only `send-message-v2`; and the binary carries `custom_prompt_view` as an identifier but no listing method. What would settle it is the interactive surface, which needs a terminal and credentials.\n\n**Attempted 2026-08-31 and it stayed `page`, with what was tried recorded so the next reader does not repeat it.** Two routes, both dead ends, and the second is the instructive one:\n\n* A deeper grep does not move it. The pinned `codex-0.151.0-linux-x64.tgz` was fetched and digest-checked, and `prompts` appears 184 times -- as an English word. The *joined* literal never appears, because a path built from a home and a name at runtime is not a literal in the artifact. This is the shape of every row still at `page`, here and in antigravity: bare-name hits of 626 for `plugins` and 1059 for `agents` prove nothing about a path.\n* Running the product did not answer either, **and it did not answer *no*.** A marker prompt was planted at `$CODEX_HOME/prompts/nddev-marker.md` with a control at `$HOME/.codex-not-a-home/prompts/`, and `codex debug prompt-input` returned **neither**. `codex doctor` reports config, MCP and runtime and names no prompt or hook. An instrument silent about the control as well as the subject has measured nothing -- reading the marker's zero alone would have produced a false negative about a surface the vendor documents.\n\nSo the row is correct at `page`, which is what `page` is for."
      },
      {
        "path": "agents",
        "kinds": [
          "agent"
        ],
        "shape": "directory",
        "source": "https://github.com/openai/codex/blob/rust-v0.151.0/codex-rs/agent-roles/src/discovery.rs; routing measured by running the 0.151.0 binary against a temporary CODEX_HOME",
        "evidence": "ran",
        "note": "$CODEX_HOME/agents/ -- scanned recursively for `*.toml`, and every file not already named by a `[agents.<name>]` stanza is loaded as a standalone role. So one file in this directory is one role, which is exactly what a component of one kind installs in one namespace.\n\n**This row carried `kinds: []` from 2026-08-28 to 2026-08-30, and the reason was wrong in a way its own measurement could not reveal.** It said a role is irreducibly two artifacts -- a stanza plus the layer it points at -- and that no component of one kind could state that route. The measurement behind it planted an `agents/<name>.md` beside a working stanza and observed that nothing loaded it and nothing complained. The product's `collect_agent_role_files` filters on `extension == \"toml\"`, so a `.md` in the right directory is invisible whatever the truth is: **the negative control could not have failed.** A conclusion was drawn from it anyway, and it was written as the stronger half of the argument.\n\n**Re-measured 2026-08-30 against the 0.151.0 artifact on linux/x86_64** -- `codex-0.151.0-linux-x64.tgz`, sha256 checked against this baseline's own software table before extracting -- `env -i` with a temporary HOME and CODEX_HOME, read back through `codex doctor`, which prints and counts startup warnings. **One platform, named because the rows below are about a scan and a filename filter, and neither macOS nor Windows was driven.** Case folding and path separators are exactly where a role's identity could differ, and this record does not answer for them:\n\n  agents/nddev-complete.toml (name, description, developer_instructions) -- accepted, silent\n  agents/nested/nddev-nested.toml -- accepted, so the scan is recursive\n  missing name -- \"must define a non-empty `name`\"\n  missing description -- \"role `nddev-nodesc` must define a description\"\n  missing developer_instructions -- \"must define `developer_instructions`\"\n  invented/nddev-invented.toml -- silent, so the scan is not directory-blind\n  agents/nddev-md-shape.md -- silent, which is the row above that failed in 2026-08-28\n\nReproduced independently by the consumer's own session against the same binary before either side changed anything.\n\n**Three required fields, and `developer_instructions` is the one a reader omits** -- the vendor's page does not present it as required. The stanza form still works and excludes its own file from the scan, so a setup may still own the pair. This provider's toolkit shipped that way until 2026-08-30 and ships one standalone file now, so the worked example is the kind this provider declares rather than a mechanism beside it."
      }
    ],
    "declined": [
      {
        "path": "skills",
        "reason": "Codex does read personal skills from here, and this record used to say it did not. Measured 2026-08-28 by installing the product through this provider and driving it with `debug prompt-input`, which renders the model-visible skills list: a SKILL.md at $CODEX_HOME/skills/<name>/ is listed, with that locator. The earlier reason -- \"Codex has no skills directory under its own home\" -- was read off the page and the page does not say it; it lists where skills are found and is silent about this one. A negative taken from a document that never made it. Declined all the same, for two reasons that compound. First, no page says so: learn.chatgpt.com/docs/build-skills names the working directory and its parents to the repository root, $REPO_ROOT/.agents/skills, $HOME/.agents/skills, /etc/codex/skills, and skills bundled by OpenAI -- not this; and github.com/openai/codex/blob/main/docs/skills.md links to developers.openai.com/codex/skills, which redirects back to that same page, so there is one source reached two ways rather than two sources. Second, the product manages this directory: its own bundled skills materialise at skills/.system/, and deleting that directory entirely made the product rebuild all six on the next start. Owning `skills` would make `remove` delete the product's own along with ours, and the only evidence that this survives is the product's willingness to recover -- behaviour rather than a promise, true until the product changes. The documented user-level path is $HOME/.agents/skills, outside any product's configuration home, which is what the consumer's ADR-0127 proposes a scope for.",
        "source": "measured through launch; learn.chatgpt.com/docs/build-skills lists six locations and not this one"
      },
      {
        "path": ".agents/skills",
        "reason": "The user-scope skills directory is $HOME/.agents/skills -- a sibling of ~/.codex, not a child of it. Declared relative to this provider's target it resolves to ~/.codex/.agents/skills, which Codex never reads. Same shape as the pi managed_paths defect.",
        "source": "https://learn.chatgpt.com/docs/build-skills"
      },
      {
        "path": "plugins",
        "reason": "Codex plugins are drawn from remote marketplaces shared with ChatGPT, not from a folder under the Codex home. Re-checked against the official rust-v0.153.0 release notes on 2026-09-03: the new plugin CLI can list, install and remove plugins from remote marketplaces. Re-checked against rust-v0.153.1 on 2026-09-04: that tag only backports GPT-6-Astra into the API model catalog, without adding it to the picker and without a new provider-owned path. That is an acquisition command and product-managed state, not a new provider-owned projection path, so the decline remains correct and the nddev-builder must not manufacture a local plugins namespace.",
        "source": "https://learn.chatgpt.com/docs/codex/cli"
      },
      {
        "path": "AGENTS.override.md",
        "reason": "Codex reads this **before** AGENTS.md at global scope and takes only the first non-empty file at that level, so a home holding a non-empty one ignores the instruction file this provider installs. An empty override does not silence the floor -- Codex skips empty files -- which is worth knowing before concluding that a present override is the reason instructions are not applying. Deliberately not owned: an override exists so a person can escape a managed floor, and a provider that owned it could remove the escape with `remove`.",
        "source": "https://learn.chatgpt.com/docs/agent-configuration/agents-md"
      },
      {
        "path": "NDDEV-CODEX-PROVIDER.json",
        "reason": "This provider's own state file: which setup is applied, the identity it recorded, and which slot reverses the last operation. Written by every operation and excluded from target identity, because counting it would leave a target different from the identity the operation just wrote. Not a projection surface and never ownable as one.",
        "source": "this provider's own contract; no vendor page is involved"
      },
      {
        "path": ".codex-setup-system",
        "reason": "This provider's own control directory: the target lock, the backup slots and their payloads. Kept out of the declaration for the same reason as the state file, and recorded here because the declined list is where a reader looks before opening a file to find out what it is.",
        "source": "this provider's own contract; no vendor page is involved"
      },
      {
        "path": "managed-config",
        "reason": "Not a path in the target, and named without an extension for that reason: this product's managed policy lives at a **system** path, and every recorded path here is relative to the target. Three literals in the 0.149.1 binary:\n\n  * `/etc/codex/managed_config.toml` — the administrator's policy\n  * `/etc/codex/requirements.toml` — the clamp above it\n  * `/etc/codex/config.toml` — a system-wide layer below both\n\nThe same build carries `legacy_managed_config_file`, `legacy_managed_config_mdm` and `allow_managed_hooks_only`, so the policy also arrives by MDM and can restrict which hooks may run at all. No macOS or Windows equivalent appears as a literal in this build, and none is claimed here: absence of a literal is not absence of a path, and the honest record is what was measured.\n\n**It bears on the `full-auto` posture**, exactly as the same surface does on the two harnesses that already record one. That setup writes a permissive approval policy and sandbox mode into the owned `config.toml`; under a managed policy those keys are correct, at a correct path, in a file the product reads — and a higher layer overrides them. Install, verify and restore all succeed and nothing about what the product permits has changed.\n\nRecorded and never touched. It needs root to write, it is outside the configuration home this provider is given, and owning an organisation's policy is the defect this estate has already shipped once: on the harness next door, owning a signed policy deleted it and kept its signatures, which is the one state that product's own gate refuses.\n\n**Corrected 2026-08-30: the sentence above measured one build and spoke about the product.** It said *\"no macOS or Windows equivalent appears as a literal in this build, and none is claimed here\"*, which was true and was read as a fact about codex rather than about a linux artifact. Windows paths are compiled out of a linux binary, so that search could only ever return zero.\n\nAsked of the Windows artifact instead -- `codex-0.151.0-win32-x64.tgz`, sha256 `9044e644…d7355` checked against this baseline's own table before extracting, `package/vendor/x86_64-pc-windows-msvc/bin/codex.exe`:\n\n  OpenAI\\Codex\\requirements.toml   6\n  OpenAI\\Codex                      6\n  ProgramData                       7\n  OpenAI\\Codex\\config.toml          0\n  NddevInventedDir                  0   (control)\n  nddev-invented                    0   (control)\n\nSo the managed **requirements** path is in the shipped Windows bytes as a joined literal, the config path beside it is composed at runtime and does not appear as one -- the same reason every runtime-joined path in this record reads `page` -- and the invented controls return zero, so the search discriminates. The pinned source agrees: `config/src/loader/mod.rs` at `rust-v0.151.0` documents `%ProgramData%\\OpenAI\\Codex\\requirements.toml` and `…\\config.toml` as the Windows system layers, `/etc/codex/…` as the Unix ones, and carries a `#[cfg(target_os = \"macos\")]` branch reading a managed requirements payload delivered by MDM.\n\nStill declined, and for the reason it always was: these are **system** paths, outside any target this provider is given. What changes is that the record no longer implies the product lacks them.",
        "source": "measured in the 0.149.1 binary; https://learn.chatgpt.com/docs/config-file/config-reference"
      },
      {
        "path": "mcp.json",
        "reason": "Codex keeps MCP servers under `[mcp_servers.<name>]` in `config.toml`, which this provider owns and writes and restores whole -- so MCP is already covered by the `setting` kind. **A key inside a file is not a projection surface**: declaring `mcp` here would promise to install, observe and roll back a fragment of a document, which nothing in this program does.\n\nThere is no `mcp.json` under this home. The 0.150.1 binary carried the literal twenty-two times and every one was about a *plugin's* servers -- `selected_executor_plugin_mcp.mcp.json` beside `.codex-plugin/plugin.json`, `.claude-plugin/plugin.json` and `.cursor-plugin/plugin.json`. Recorded because the string is there and a reader who greps for it will find it.\n\n**Re-asked of the 0.151.0 linux/x86_64 bytes on 2026-08-30, because a decline is only as current as the release it was taken against.** Twenty-three occurrences now, still every one a plugin context; `.codex/mcp.json` returns zero, and two invented paths searched in the same run return zero, so the search discriminates. `mcp_servers` appears forty times. The decline stands and is now measured against what this tree pins.\n\nRe-measured deliberately rather than left alone: the `agents` row above declined a kind on a 0.149.1 reading that 0.151.0 had already made false, and this is the other kind this harness withholds. A decline nobody re-asks is the shape that defect had.\n\nOne false lead resolved while doing it, recorded so the next reader does not chase it: the strings table places `agents/openai.yaml` next to these literals, which would be alarming if it were a path under this home -- `agents` is a namespace this provider removes whole. It is not. It is `skill-creator/agents/openai.yaml`, an asset inside the product's own bundled sample skill, plus a capability-discovery message. Nothing writes into `$CODEX_HOME/agents` but a person and this provider.",
        "source": "measured from the pinned artifact, digest verified before reading (codex 0.150.1); https://learn.chatgpt.com/docs/config-file/config-reference"
      }
    ],
    "config_home_env": "CODEX_HOME",
    "config_home_env_note": "Measured 2026-08-28: launched through this provider, the product named the target as its own `codex_home` in its output.",
    "scoped": [
      {
        "target_scope": "user_root",
        "root": "~/.agents",
        "surfaces": [
          {
            "path": "skills",
            "kinds": [
              "skill"
            ],
            "shape": "directory",
            "source": "https://learn.chatgpt.com/docs/build-skills, and measured by running the 0.150.1 product against a temporary HOME, 2026-08-29",
            "evidence": "ran",
            "note": "The documented user-level skills directory is $HOME/.agents/skills. Relative to this scope's own root it is `skills`, not `.agents/skills` -- the root is what the scope names, and writing it into the path again would put the skills at ~/.agents/.agents/skills. Codex also reads $CODEX_HOME/skills, measured, which is declined in the global block and for reasons recorded there.\n\n**Run, not read.** The pinned `codex-0.150.1-linux-x64.tgz` was fetched, its digest checked against the artifact table, and the binary driven with `debug prompt-input` against a temporary `HOME` and `CODEX_HOME`. A `SKILL.md` at `$HOME/.agents/skills/nddev-user-root-probe/` appears in the rendered `<skills_instructions>` list with that file locator, beside the product's own five bundled skills.\n\n**With a control, because a positive without one says nothing.** A second skill was placed at `$HOME/.agents-not-a-root/skills/` -- a sibling root no page names -- and it is absent from the same listing. So the product is reading the documented root rather than scanning `$HOME` broadly, which is the reading a bare positive could not distinguish."
          }
        ],
        "declined": [
          {
            "path": "agents",
            "reason": "No page names a user-level agents directory under the .agents convention. learn.chatgpt.com/docs/build-skills documents skills at $HOME/.agents/skills and says nothing about siblings; a declared kind is a promise of a rollback, and this one would promise a rollback of a directory nothing reads.",
            "source": "https://learn.chatgpt.com/docs/build-skills"
          },
          {
            "path": "AGENTS.md",
            "reason": "Instructions at user level are ~/.codex/AGENTS.md, which the global scope already owns. A second copy under the convention root would be a second statement of one fact, and nothing documents Codex reading it there.",
            "source": "https://learn.chatgpt.com/docs/build-skills"
          }
        ]
      },
      {
        "target_scope": "project",
        "root": "project root",
        "surfaces": [
          {
            "path": "AGENTS.md",
            "kinds": [
              "instruction"
            ],
            "shape": "file",
            "source": "https://developers.openai.com/codex/guides/agents-md; codex-0.153.0-linux-x64.tgz sha256:856f408ea61b44a381b7d6fb7c82365dfcef649ae2a340fc01282cf63c30cd8a, run 2026-09-03",
            "evidence": "ran",
            "note": "Measured by running the digest-verified 0.153.0 binary with `debug prompt-input` in a temporary project. NDDEV_PROJECT_MARKER_153 in project-root AGENTS.md appeared once; a marker in an invented HOME/AGENTS.md appeared zero times; after moving the project file away its marker appeared zero times. This discriminates the project surface from a broad home scan and from output independent of the file."
          },
          {
            "path": ".codex/hooks.json",
            "kinds": [
              "hook"
            ],
            "shape": "file",
            "source": "https://developers.openai.com/codex/hooks; path and project scope carried by the Codex 0.153.0 hook configuration implementation",
            "evidence": "page",
            "note": "The project hook manifest retains its .codex parent under the repository root. Trust and handler approval remain product concerns; this provider owns exact bytes and restores them, but does not claim that installing a file grants execution trust."
          }
        ],
        "declined": [
          {
            "path": "AGENTS.override.md",
            "reason": "This is an intentional project-local override. A setup owning it could silently suppress the shared AGENTS.md it installs.",
            "source": "https://developers.openai.com/codex/guides/agents-md"
          }
        ]
      }
    ],
    "configuration_format": {
      "file": "config.toml",
      "grammar": "toml",
      "accepts_comments": true,
      "note": "TOML, so `#` comments are part of the grammar. No JSON schema is possible for it and none is expected; the setups are checked by parsing instead.",
      "measured_at": "2026-08-28"
    },
    "projection_basis": {}
  },
  "product_permission_profiles": {
    "configuration_key": "default_permissions",
    "custom_profiles_surface": "[permissions.<name>]",
    "custom_profiles_used": false,
    "managed_enforcement": false,
    "minimum_codex_version_ref": "build/version.json:codex_permission_profiles_since",
    "status": "beta",
    "note": "This names what the Codex product does about permissions -- a beta feature of the product, read from its own documentation. It is NOT this provider's protocol field of the same name, which is permission_profiles in provider-info and is [\"default\"] on every one of the seven. The two share a word and name different axes. Renamed rather than annotated in place, for the reason docs/CONSUMER-GAPS.md records the last time this estate met the pattern: the shared name is the trap, and no amount of documentation survives the next reader."
  },
  "software_artifacts": {
    "command": "codex",
    "shape": "gzip-tar",
    "platforms": {
      "linux/arm64": {
        "url": "https://registry.npmjs.org/@openai/codex/-/codex-0.153.1-linux-arm64.tgz",
        "bytes": 121697093,
        "sha256": "sha256:3e050251b40b334e74a27c52128b86f8468fa8242d5c8205b04f4b31f85690f7",
        "member": "package/vendor/aarch64-unknown-linux-musl/bin/codex"
      },
      "linux/x86_64": {
        "url": "https://registry.npmjs.org/@openai/codex/-/codex-0.153.1-linux-x64.tgz",
        "bytes": 129241027,
        "sha256": "sha256:33b4ae16df4530361a651bc1540ae8de325ccc789622a812c94585bdf205f7f4",
        "member": "package/vendor/x86_64-unknown-linux-musl/bin/codex"
      },
      "macos/arm64": {
        "url": "https://registry.npmjs.org/@openai/codex/-/codex-0.153.1-darwin-arm64.tgz",
        "bytes": 115673114,
        "sha256": "sha256:33babfe9db041ff18100775bf34df673c1baf9a5d7b1d09fdcafefc58501aa87",
        "member": "package/vendor/aarch64-apple-darwin/bin/codex"
      },
      "macos/x86_64": {
        "url": "https://registry.npmjs.org/@openai/codex/-/codex-0.153.1-darwin-x64.tgz",
        "bytes": 123566510,
        "sha256": "sha256:bafcac30746296be53a183ff907b9ae59cef9f8e65fa511abcb7661bd247bf80",
        "member": "package/vendor/x86_64-apple-darwin/bin/codex"
      },
      "windows/arm64": {
        "url": "https://registry.npmjs.org/@openai/codex/-/codex-0.153.1-win32-arm64.tgz",
        "bytes": 132174290,
        "sha256": "sha256:5ee2fab96e2ba898ea7e4c0c8f4222f4dfd08d0ea9b4c9b849323ebb1e90ef14",
        "member": "package/vendor/aarch64-pc-windows-msvc/bin/codex.exe"
      },
      "windows/x86_64": {
        "url": "https://registry.npmjs.org/@openai/codex/-/codex-0.153.1-win32-x64.tgz",
        "bytes": 141469181,
        "sha256": "sha256:fa9e2ac23e905fdcba1fb4f396c57da65fac04c1a3ee4eebbbb85b5ad6b2dc10",
        "member": "package/vendor/x86_64-pc-windows-msvc/bin/codex.exe"
      }
    },
    "version": "0.153.1",
    "verified_at": "2026-09-03T22:15:16+00:00"
  },
  "setup_catalogue_digest": "sha256:204d778fa296d8cb67c14fce8de807c461737ca1ada580660c8102269c4d7ddb",
  "previous_software_artifacts": {
    "command": "codex",
    "shape": "gzip-tar",
    "platforms": {
      "linux/arm64": {
        "url": "https://registry.npmjs.org/@openai/codex/-/codex-0.153.0-linux-arm64.tgz",
        "bytes": 121658947,
        "sha256": "sha256:1c65fda30a919908688147b67b1b195ab635bd4a5eac49ffa7be527a13af0a09",
        "member": "package/vendor/aarch64-unknown-linux-musl/bin/codex"
      },
      "linux/x86_64": {
        "url": "https://registry.npmjs.org/@openai/codex/-/codex-0.153.0-linux-x64.tgz",
        "bytes": 129210185,
        "sha256": "sha256:856f408ea61b44a381b7d6fb7c82365dfcef649ae2a340fc01282cf63c30cd8a",
        "member": "package/vendor/x86_64-unknown-linux-musl/bin/codex"
      },
      "macos/arm64": {
        "url": "https://registry.npmjs.org/@openai/codex/-/codex-0.153.0-darwin-arm64.tgz",
        "bytes": 115615644,
        "sha256": "sha256:ce03d32fe1cb0c4a02bf86a139a43519bb791c2ba6fe56d665c34fcadcbc7c00",
        "member": "package/vendor/aarch64-apple-darwin/bin/codex"
      },
      "macos/x86_64": {
        "url": "https://registry.npmjs.org/@openai/codex/-/codex-0.153.0-darwin-x64.tgz",
        "bytes": 123457960,
        "sha256": "sha256:c636191070281854d869b3ff3f6efe506db81c9eb7035930440c8afb29d626a4",
        "member": "package/vendor/x86_64-apple-darwin/bin/codex"
      },
      "windows/arm64": {
        "url": "https://registry.npmjs.org/@openai/codex/-/codex-0.153.0-win32-arm64.tgz",
        "bytes": 132156833,
        "sha256": "sha256:e390adc8ef145ea5bf0b687408b0d36ed9abdf879b41265148e3474f4bc81fad",
        "member": "package/vendor/aarch64-pc-windows-msvc/bin/codex.exe"
      },
      "windows/x86_64": {
        "url": "https://registry.npmjs.org/@openai/codex/-/codex-0.153.0-win32-x64.tgz",
        "bytes": 141426872,
        "sha256": "sha256:30f28ce47e7b1c9ba79ceefb60ba9902253f5a1ec831dedc75f120e99e138bdb",
        "member": "package/vendor/x86_64-pc-windows-msvc/bin/codex.exe"
      }
    },
    "version": "0.153.0",
    "verified_at": "2026-09-03T14:02:48+00:00"
  },
  "capability_features": {
    "measured_at": "2026-08-30",
    "measured_against": "0.151.0",
    "artifact_sha256": "sha256:b3bcf2c11693d7c8155de637dd6562ba19d916ba13471a7c8737de55e5328fc6",
    "evidence": "ran",
    "source": "https://github.com/openai/codex/blob/rust-v0.151.0/codex-rs/features/src/lib.rs",
    "registry_size": 126,
    "stable": 40,
    "stable_and_off_by_default": [
      "memories",
      "multi_agent_v2",
      "recommended_plugins"
    ],
    "note": "The three keys `full-auto` sets under `[features]`, and how the set was chosen rather than picked. The 0.151.0 registry carries 126 `FeatureSpec` entries; 40 are `Stage::Stable` and exactly three of those are `default_enabled: false` -- `memories`, `multi_agent_v2` and `recommended_plugins`. Everything else stable is already on, and the remainder are under development, deprecated or removed, which is behaviour this build does not stand behind and a posture should not switch on.\n\n**Ran, with the control that makes the number mean something.** The artifact above -- the linux/x86_64 one -- was fetched, its sha256 checked against this baseline's own software table before it was extracted, and driven with `env -i` and a temporary HOME and CODEX_HOME. The registry itself is source and platform-independent; the counts read back are one platform's, named rather than generalised. `codex doctor` reports `46 enabled · 0 overridden` for an empty configuration file, and `49 · 3` for these three -- so the reading is a difference this instrument can show, not a constant it prints either way.\n\n**This block exists because a comment could not hold it.** The keys used to be argued for in prose beside the value, in a sentence saying that the 0.149.1 binary then pinned carried none of those three spellings. All three exist at 0.151.0 -- `hooks` and `multi_agent` stable and on, `memories` stable and off -- so the sentence outlived what it described by two pins, and nothing could read it to notice. Re-measure when the pin moves; the versions here are a record of one day rather than a claim about today."
  },
  "configuration_schema": {
    "url_template": "https://github.com/openai/codex/releases/download/rust-v{version}/config-schema.json",
    "applies_to": [
      "config.toml"
    ],
    "measured_at": "2026-08-30",
    "evidence": "ran",
    "note": "This product publishes a JSON Schema for its own configuration file as a release asset, generated from the same Rust types it parses with, and it is `additionalProperties: false` at the top level and inside `ToolsToml`. So the vendor will say, in machine-readable form, whether a key we write is a key it has.\n\n**A version template rather than a url.** `{version}` is filled from `software_artifacts.version`, so the schema fetched is always the schema of the release this tree pins. A literal url would be a second place the version lives, and a pin refresh would move one and not the other -- which is the shape of the defect this block was added after.\n\n**Red-first, against the vendor's own answer.** The `full-auto` file this replaced wrote `view_image` under `[tools]`, and validating it against this schema returns *\"Additional properties are not allowed ('view_image' was unexpected)\"* at `['tools']`; the corrected file validates. Nothing in this repository could say that before, because a `config.toml` has no `$schema` line to name and the schema sweep only looked at files that carry one."
  },
  "surface_presence": {
    "measured_at": "2026-09-02",
    "artifact": "https://registry.npmjs.org/@openai/codex/-/codex-0.152.1-linux-x64.tgz",
    "artifact_sha256": "sha256:0ed4978e80117a5e203a436026c37276029a3642d633b6916ab45143d10565cd",
    "product_version": "0.152.1",
    "declared_surfaces": 6,
    "present": 6,
    "absent": [],
    "control": "zzq-invented-surface-nobody-ships",
    "control_hits": 0,
    "method": "Every namespace this harness declares, global and scoped, searched as a fixed string in the artifact the current pin names, digest verified before reading; both the declared path and its last segment counted, because a product that composes a path by joining segments never spells the whole of it. A count here is a lower bound and not a routing proof -- what it answers is whether a surface vanished from the product under a version bump, which five of the seven took on 2026-09-02. The invented control was searched in the same run and found nowhere, so the search discriminates."
  }
}
